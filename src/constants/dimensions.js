import { Dimensions } from 'react-native';

const WINDOW_WIDTH = Dimensions.get('window').width;
const WONDOW_HEIGHT = Dimensions.get('window').height;

const DIMENSIONS = {
  WINDOW_WIDTH,
  WONDOW_HEIGHT
};

export default DIMENSIONS;
